// Stub for imports that reference paths outside the psychojs package
// (../../../components/..., ../../../threshold.js, etc.)
// These are resolved at build time in the website context but don't exist here.
export default {};
